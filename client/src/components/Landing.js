// Landing component , adjust later
import React from "react";

// first{}:show some JS
// second{}: some JS object
const Landing = () => {
  return (
    <div style={{ textAlign: "center" }}>
      <h1>Emaily!</h1>
      Collect some feedbacks from your users. Need to adjust later
    </div>
  );
};

export default Landing;
